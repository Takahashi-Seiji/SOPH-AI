<turbo-frame id="results-frame">
  <div id="correct-answers">
    <h1>Your results</h1>
    <h2>Your total score for this quiz on <%= lecture.title %> was: <%= quiz.grade %>/ 10</h2>
  </div>

  <div>
    <p>Quizzes submitted: <%= @student_lecture.quiz_submissions_count %>/5Quizzes submitted: 2/3</p>
    <h3> </h3>
    <div>
      <%= " 📝" * 10 %>
    </div>
    <ul>
      <% quiz.questions.each do |question| %>
        <li>
          <%= question.title %>
          <% if question.correct? %>
            <span>✅: <%= question.correct_answer_value %> - <%= question.correct_answer_label %></span>
          <% else %>
            <span>❌ Correct answer: <%= question.correct_answer_value %> - <%= question.correct_answer_label %></span>
          <% end %>
        </li>
      <% end %>
    </ul>
  </div>
</turbo-frame>
